export interface Config {
  /**
  * Visible in frontend and backend
  * @visibility frontend
  */
  localhost?: {
    /**
    * Visible in frontend and backend
    * @visibility frontend
    */
    user: string;
    /**
    * Visible in frontend and backend
    * @visibility frontend
    */
    userEntityRef: string;
  },
}
