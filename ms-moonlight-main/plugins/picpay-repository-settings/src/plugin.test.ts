import { picpayRepositorySettingsPlugin } from './plugin';

describe('picpay-repository-settings', () => {
  it('should export plugin', () => {
    expect(picpayRepositorySettingsPlugin).toBeDefined();
  });
});
